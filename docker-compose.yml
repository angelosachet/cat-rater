services:
  cat-rater:
    build: .
    ports:
      - "3007:3007"
    volumes:
      # Volume para as fotos - coloque suas fotos aqui
      - ./public/photos:/app/public/photos
      # Volume para os votos (persistência)
      - ./data:/app/data
    environment:
      - PORT=3007
    restart: unless-stopped
    container_name: cat-rater-app

volumes:
  photos:
  data:
